interface:
  display_name: "WorkBuddy Theme Manager"
  short_description: "Apply safe local WorkBuddy themes through chat."
  default_prompt: "Use $workbuddy-theme-manager to inspect WorkBuddy, prepare a theme, and apply it safely."
